import { Grid } from '@mui/material';
import React from 'react';

export const EducationExperience:React.FC = () => {
    return (
        <Grid>
            EducationExperience
        </Grid>
    )
}